# OpenAL Soft 1.19.1 reads app0:/alsoft.conf before alcOpenDevice returns.
# Sound::Manager owns 64 names; the other 16 are reserved for two seven-stream
# music sets, the jingle actor, and Theora. The runtime checks this exact device
# capacity before alcCreateContext can reserve voices.
[general]
sources = 80
